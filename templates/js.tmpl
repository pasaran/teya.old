module
    function( jpath ) {
        var m0 = {};

        %vars

        %defs

        module.exports = function( data, id ) {
            return m0[ id ]( data );
        };
    }

def_template
    //  template

def_var
    //  %name : %get_type()
    var %name = %value:value;
